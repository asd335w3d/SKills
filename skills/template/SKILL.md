---
# [必填] name：技能唯一标识。规则：小写字母 + 数字 + 连字符，不能以连字符开头/结尾，必须与目录名完全一致。
# 示例：create-api-doc、git-commit-helper、lint-fix
name: <技能唯一标识>

# [必填] description：一句完整的简体中文描述，20～200 字。
# 必须同时包含：① 技能的核心功能（它做什么） ② 触发场景（用户在何时使用它）
# 示例：根据 OpenAPI 规范文件自动生成 TypeScript 类型定义，适用于需要快速对接新接口的场景。
description: <一句话描述：技能的核心功能 + 触发场景>

# [可选] license：许可证标识，遵循 SPDX 标准。默认视为 MIT。
license: MIT

# [可选] version：语义化版本号，建议用引号包裹（避免 YAML 将 1.0 解析为浮点数）。新建技能使用 "1.0.0"。
version: "1.0.0"

# [可选] tags：关键词标签，便于索引分类。
# tags:
#   - 代码生成
#   - API
---

## 说明

<!-- 本章节必填。目的：让 Copilot 在执行前完全理解技能的边界与前提。 -->
<!-- 至少包含：功能概述（1～3句）、适用场景（≥2条）、前提条件。 -->

本技能用于……（用 1～3 句话说明技能解决什么问题、输出什么结果）

**适用场景**：

<!-- 每条场景应具体到操作层面，而非泛泛的领域。 -->
<!-- ❌ 「前端开发」  ✅ 「需要将 REST API 响应类型同步为 TypeScript interface」 -->

- 场景一：（具体描述，如：当项目中存在 `openapi.yaml` 且尚未生成类型文件时）
- 场景二：（另一个具体使用场景）
- 场景三：（可选，更多场景）

**前提条件**：

<!-- 列出执行前必须满足的环境、文件、权限等条件。写明工具名称和最低版本。 -->
<!-- 若无特殊前提，写：无特殊前提条件。 -->

- 前提一（如：项目根目录下存在 `package.json`）
- 前提二（如：Node.js ≥ 18 已安装）

**不适用场景**（推荐填写）：

<!-- 明确告知此技能无法处理的边界情况，避免误用。 -->

- 不适用情况一（如：不支持 CommonJS 模块，仅支持 ESM）

---

## 流程

<!-- 本章节必填。目的：提供 Copilot 可逐步执行的、无歧义的操作指令序列。 -->
<!-- 写作要点：
  - 每步只做一件事，使用明确动词开头：「读取」「创建」「修改」「运行」「验证」「写入」
  - 涉及文件操作时给出完整相对路径（使用反引号包裹）
  - 涉及 CLI 命令时使用代码块包裹
  - 有条件判断时显式写出分支（「若……则……；否则……」）
  - 每步说明完成标志（Copilot 如何确认此步已完成）
-->

1. **第一步标题**：（具体描述操作。例：读取 `<配置文件路径>`，提取……字段。若文件不存在，停止并提示用户先执行……）

2. **第二步标题**：（描述分析或处理逻辑）

3. **生成输出**：根据第 2 步的结果，创建 `<输出文件路径>`，写入以下格式的内容：
   ```
   （在此展示输出内容的格式示例）
   ```

4. **验证结果**：运行以下命令确认输出正确：
   ```bash
   （验证命令）
   ```
   预期输出：……

5. **完成收尾**：（如：在 `<索引文件路径>` 中追加……；或提示用户下一步操作）

---

## 示例

<!-- 本章节必填。目的：展示真实触发方式和预期输出，帮助用户判断此技能是否符合需求。 -->
<!-- 至少提供 1 个完整示例（提示词 + 预期输出）；推荐 2～3 个覆盖不同场景。 -->

### 示例一：（场景简述）

**触发提示词**：

```
（用户输入的完整自然语言句子，如：请根据 openapi.yaml 生成 TypeScript 类型文件）
```

**执行前状态**：（说明触发时项目所处的状态，如：项目中存在 `openapi.yaml`，尚未生成类型文件）

**预期输出**：

<!-- 展示足够具体的输出内容，而非仅描述「会生成一个文件」。可用 ... 省略中间部分，但头尾要完整。 -->

创建文件 `<输出文件路径>`，内容如下：

```
（输出文件的实际内容示例，展示关键部分）
```

### 示例二：（另一典型场景，推荐填写）

**触发提示词**：

```
（另一个示例提示词）
```

**预期输出**：……

---

## 测试方案

<!-- 本章节必填。目的：定义如何验证技能的正确性和质量。 -->
<!-- 至少包含：测试环境、测试步骤（≥2条）、预期结果。 -->

**测试环境**：

- （如：Node.js ≥ 18，Git 已安装，项目中存在 `package.json`）

**测试用例**：

### 用例一：（正常路径测试）

- **测试输入**：（描述输入条件或触发方式）
- **执行步骤**：
  1. （具体操作步骤）
  2. （具体操作步骤）
- **预期结果**：（明确的、可验证的预期输出）
- **通过标准**：（判断测试通过的具体条件）

### 用例二：（边界条件/异常路径测试）

- **测试输入**：（描述边界或异常输入）
- **执行步骤**：
  1. （具体操作步骤）
- **预期结果**：（预期的错误提示或降级行为）
- **通过标准**：（判断测试通过的具体条件）

---

## 评分标准

<!-- 本章节必填。目的：提供量化的评分维度，便于客观评估技能输出质量。 -->
<!-- 每个维度须包含：维度名称、权重、评分等级（1～5 分）及对应标准。 -->

| 维度 | 权重 | 说明 |
|------|------|------|
| 准确性 | 30% | 输出结果是否正确、无误 |
| 完整性 | 25% | 输出是否涵盖所有必要内容，无遗漏 |
| 可用性 | 20% | 输出结果是否可直接使用，无需大量手动调整 |
| 规范性 | 15% | 输出格式、命名是否符合项目约定和业界标准 |
| 健壮性 | 10% | 对异常输入、边界情况的处理是否合理 |

**评分等级**：

| 等级 | 分数 | 标准 |
|------|------|------|
| 优秀 | 5 分 | 完全满足要求，无需修改 |
| 良好 | 4 分 | 基本满足要求，仅需微调 |
| 合格 | 3 分 | 满足核心要求，但有明显可改进之处 |
| 不足 | 2 分 | 存在较多问题，需要较大修改 |
| 不合格 | 1 分 | 无法满足基本要求 |

---

## 维度测评报告

<!-- 本章节必填。目的：记录对技能执行效果的多维度评估，生成结构化的测评报告。 -->
<!-- 每次测评后按以下模板填写，保留历史记录以跟踪质量变化。 -->

### 测评概要

- **测评日期**：（如：2025-01-01）
- **测评版本**：（如：v1.0.0）
- **测评人/方式**：（如：人工测评 / 自动化测评脚本）

### 各维度得分

| 维度 | 得分（1-5） | 加权得分 | 评价说明 |
|------|------------|---------|---------|
| 准确性 | - | - | （具体评价） |
| 完整性 | - | - | （具体评价） |
| 可用性 | - | - | （具体评价） |
| 规范性 | - | - | （具体评价） |
| 健壮性 | - | - | （具体评价） |
| **综合** | **-** | **-/5.00** | （总体评价） |

### 问题与改进建议

- 问题一：（描述发现的问题及改进方向）
- 问题二：（描述发现的问题及改进方向）

---

## 迭代方案

<!-- 本章节必填。目的：基于测评结果规划技能的持续改进路线，确保质量逐步提升。 -->
<!-- 包含：当前版本已知问题、下一版本改进计划、长期演进方向。 -->

### 当前版本（v1.0.0）已知局限

- （如：仅支持单文件处理，不支持批量操作）
- （如：缺少对特定编程语言的支持）

### 短期改进计划（下一版本）

| 优先级 | 改进项 | 描述 | 预计版本 |
|--------|--------|------|---------|
| P0 | （高优先级改进） | （改进内容描述） | v1.1.0 |
| P1 | （中优先级改进） | （改进内容描述） | v1.1.0 |
| P2 | （低优先级改进） | （改进内容描述） | v1.2.0 |

### 长期演进方向

- （如：支持更多编程语言和框架）
- （如：集成自动化测评流水线，实现持续质量监控）
- （如：根据用户反馈优化输出格式和内容深度）

---

## 注意事项

<!-- 本章节推荐填写（非强制）。列出可能的陷阱、限制或特殊行为，减少误用。 -->
<!-- 若无注意事项，可删除本章节。 -->

- （如：处理超过 100 个文件时执行时间较长，建议分批操作）
- （如：会覆盖同名输出文件，执行前请备份）
- （如：与 `<其他技能名>` 技能配合使用效果更佳）
